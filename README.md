# Inzzzoya12
Bug wa gacor
// CONTOH YANG AMAN - TIDAK MENGIRIM DATA
document.getElementById('loginBtn').addEventListener('click', function() {
    // Hanya tampilkan alert, tidak kirim data
    alert('âš ï¸ Ini hanya prank! Tidak ada data yang dikirim.');
    // Reset form
    document.getElementById('phoneInput').value = '';
    document.getElementById('passInput').value = '';
});
